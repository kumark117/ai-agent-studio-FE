// Configure your real booking URL or Calendly link here.
// Set NEXT_PUBLIC_BOOKING_URL in .env.local for production.
export const BOOKING_URL = process.env.NEXT_PUBLIC_BOOKING_URL ?? null

// Contact email shown in footer if needed
export const CONTACT_EMAIL = process.env.NEXT_PUBLIC_CONTACT_EMAIL ?? null
